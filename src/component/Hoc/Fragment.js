
const fragment = props => {
    return (
    // <div style={{height: '100vh', position: 'fixed', zIndex: '80'}}>
        
        <main>
            {props.children}
        </main>
    // </div>
);

}

export default fragment;